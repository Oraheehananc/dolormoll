// Assuming stringArr is an array of numbers
let stringArr: number[] = [];

// Function to add a number to the array
function addNumberToArray(array: number[], numberToAdd: number): void {
  array.push(numberToAdd);
}

// Adding the number 34 to stringArr
addNumberToArray(stringArr, 34);
